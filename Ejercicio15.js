const areatorresTriangulo = (base, altura) => {
    return (base * altura) / 2;
    };
    
    const areatorresTrianguloCorto = (base, altura) =>
    (base * altura) / 2;
    
    console.log("Área:", areatorresTriangulo(10, 5));